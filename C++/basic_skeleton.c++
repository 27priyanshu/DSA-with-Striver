#include <bits/stdc++.h> //used to load all the libraries in the C++
using namespace std;     // used for not to use always a std ::

int main()
{
    string s1 ;
    string s2;
    cin >> s1 >> s2;
    cout << s1<< " "<< s2;

    return 0;
}